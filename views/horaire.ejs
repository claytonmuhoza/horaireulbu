<%- include('part/header.ejs') %> 

<body>
  <%- include('part/menu.ejs') %> 
  
    <h1>horaire <% if(campus){%>campus <%= campus.toUpperCase() %><%} %> </h1>
    <div style="overflow-x: scroll;">
    <table class="table table-striped table-bordered" style="margin: 10px">
      <tbody>
        
          
        <%  for(let i=0;i<data.length;i++)
{%><tr><%
  for(let j=0;j<data[i].length;j++)
  {%>
   <td <%if(data[i][j] && data[i][j].length>40 && i<2 && (data[i][j].search("HORAIRE") > 0 || (data[i][j].search("horaire")))){%>colspan="10" <%}%>><%=data[i][j]%></td><%
  }%></tr><%
} 
%>
        
      </tbody>
    </table>
    </div>
  </body>
</html>

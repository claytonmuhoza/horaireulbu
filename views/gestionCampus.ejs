<%- include('part/header.ejs') %> 

<body>
  <%- include('part/menu.ejs') %> 
  <div style="max-width: 400px; margin: 10px auto;">
    <form method="post" encType="multipart/form-data" class="mb-3" style="box-shadow: 1px 2px 3px #0000006b; padding: 10px;">
        <% try{if(message.error !== 'undefined' && message.error){ %>
            <div class="alert alert-danger mb-3" role="alert">
              <%=message.error%>
            </div>  
            <%} if(message.success !== 'undefined' && message.success){ %>
                <div class="alert alert-success mb-3" role="alert">
                  <%=message.success%>
                </div>  
                <% 
                
            }} catch{}%>
        <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label">Nom du campus que vous voulez ajouter</label>
          <input type="text" class="form-control" name="nomcampus" id="exampleInputEmail1">
        </div>
        <button type="submit" class="btn btn-primary">Ajouter le campus</button>
    </form>
    <form onsubmit="return confirm('Voulez-vous vraiment supprimer ce campus');" action="/gestioncampus?_method=DELETE" method="POST" class="mb-3" style="box-shadow: 1px 2px 3px #0000006b; padding: 10px;">
      <% try{if(message.error2 !== 'undefined' && message.error2){ %>
        <div class="alert alert-danger mb-3" role="alert">
          <%=message.error2%>
        </div>  
        <%} if(message.success2 !== 'undefined' && message.success2){ %>
            <div class="alert alert-success mb-3" role="alert">
              <%=message.success2%>
            </div>  
            <% 
            
        }} catch{}%>
       
      <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label">Nom du campus que vous voulez supprimer</label>
          <select class="form-select" name="nomcampus" id="">
              <% 
                try{
                  listecampus.forEach(element => {
                        %>
                        <option  value="<%= element%>"><%=element%></option>
                        <%
                    });
                }catch{
                  
                }
                    
                
            %> 
              
          </select>
        </div>
        <button type="submit" class="btn btn-primary">Supprimer le campus</button>
    </form>
    <form onsubmit="return confirm(`Voulez-vous vraiment supprimer l'horaire de ce campus`);" action="/horairecampus?_method=DELETE" method="POST" class="mb-3" style="box-shadow: 1px 2px 3px #0000006b; padding: 10px;">
      <% try{if(message.error3 !== 'undefined' && message.error3){ %>
        <div class="alert alert-danger mb-3" role="alert">
          <%=message.error3%>
        </div>  
        <%} if(message.success3 !== 'undefined' && message.success3){ %>
            <div class="alert alert-success mb-3" role="alert">
              <%=message.success3%>
            </div>  
            <% 
            
        }} catch{}%>
       
      <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label">Nom du campus que vous voulez supprimer l'horaire</label>
          <select class="form-select" name="nomcampus" id="">
              <% 
                try{
                  listecampus.forEach(element => {
                        %>
                        <option  value="<%= element%>"><%=element%></option>
                        <%
                    });
                }catch{
                  
                }
                    
                
            %> 
              
          </select>
        </div>
        <button type="submit" class="btn btn-primary">Supprimer l'horaire du campus</button>
    </form>
  </div>
</body>
</html>